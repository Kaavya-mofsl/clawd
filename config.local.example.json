{
  "//": "Copy to config.local.json to override anything in config.json. Git-ignored, so your settings survive `git pull`. Only include the keys you actually want to change — `strip` and `behavior` merge key by key, `profiles` replaces the whole list. Restart Clawd after editing.",

  "//profiles": "A second pet, for people running two Claude accounts as the same app under --user-data-dir. `match` is any distinctive substring of that path. Delete this block if you have one account.",
  "profiles": [
    { "id": "account1", "label": "Claude (work)", "match": null, "color": "#D97757", "lane": 0 },
    { "id": "account2", "label": "Claude (personal)", "match": ".claude-instances/account2", "color": "#5B8DEF", "lane": 1 }
  ],

  "behavior": {
    "//": "Cheaper animation on an old machine, and a longer fuse before it nudges you.",
    "fps": 10,
    "fpsAsleep": 4,
    "nagDelayMs": 20000
  }
}
